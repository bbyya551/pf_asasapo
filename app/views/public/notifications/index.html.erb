<% notifications = @notifications.where.not(visitor_id: current_user.id) %>
  <% if notifications.exists? && unchecked_notifications.any? %>
    <%= render notifications %>
      <!--binding.pry-->
    <%#= paginate notifications %>
  <% else %>
    <p>通知はありません</p>
  <% end %>