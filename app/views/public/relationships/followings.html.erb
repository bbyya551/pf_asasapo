<div class="container px-5 px-sm-0">
  <h2>フォロー中</h2>
  <div class="row">
    <div class="col-md-8">
      <% if @users.exists? %>
        <%= render 'public/users/index', users: @users %>
      <% else %>
        <p>フォローしている人はいません</p>
      <% end %>
    </div>
  </div>
</div>